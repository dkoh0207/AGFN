# AGFN runtime requirements (curated from src/ import scan, not a pip freeze).
# Loaded by setup.py as install_requires. Keep this file minimal — pip will
# resolve transitive deps. Torch + PyG stack are installed via requirements_dev.txt.

# Core scientific
numpy>=1.26
scipy>=1.11
pandas>=2.2
scikit-learn>=1.4
networkx>=3.2

# Chem / molecular
rdkit>=2024.3
datamol>=0.12
meeko>=0.5
useful_rdkit_utils>=0.3
# PyTDC is intentionally excluded here — its declared deps pin pandas==2.1.4,
# descriptastorus==2.5.0.23, rdkit-pypi, and cellxgene-census, which cannot
# co-resolve on Python 3.12. It is used only by `src/utils/metrics_utils.py`
# for the optional "tdc div" metric. Install separately with:
#     pip install --no-deps PyTDC
# (its runtime needs at the Evaluator call site are satisfied by rdkit + pandas
# already pulled in above.)

# Utilities
tqdm>=4.66
easydict
more_itertools
PyYAML>=6
requests>=2.31

# Notebook visualization (src/apps/docking/denovo/visualize.ipynb)
py3Dmol>=2.0
